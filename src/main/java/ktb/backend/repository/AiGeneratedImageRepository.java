package ktb.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AiGeneratedImageRepository extends JpaRepository<AiGeneratedImageRepository, Long> {
}
