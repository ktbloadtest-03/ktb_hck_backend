package ktb.backend.repository;

import ktb.backend.entity.LocationMarkerImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LocationMarkerImageRepository extends JpaRepository <LocationMarkerImage, Long> {
}
